# =============================================================================
# AlbLingo Backend - Production Requirements (Render Compatible)
# =============================================================================

# Core Framework
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Authentication & Security (pure Python - no Rust compilation needed)
passlib==1.7.4
bcrypt==4.0.1
PyJWT==2.8.0

# Utilities
python-multipart==0.0.6
python-dotenv==1.1.1
requests==2.31.0

# Text-to-Speech (Google TTS - works on all platforms)
gtts==2.4.0

# Image Processing
pillow==10.0.0

# AI/LLM Services
openai==1.3.0
httpx==0.25.0

# Production ASGI server
gunicorn==21.2.0

# =============================================================================
# NOTE: Using PyJWT instead of python-jose[cryptography] to avoid Rust builds
# NOTE: Removed anthropic (has Rust dependencies) - use OpenAI for AI features
# =============================================================================

